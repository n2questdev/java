package trees;

import fruits.Apple;

public class AppleTree {
	
	//Class and Object initialization sequence
	
	/*
	 * Find the class (where it is, which package)
	 * Does the class have any static initializer? If yes, execute the static initializer code
	 * Look for the constructor being used? If defined, execute it
	 * 
	 * 
	 * 
	 * 
	 */
	
	Apple[] apples = new Apple[10];

}
